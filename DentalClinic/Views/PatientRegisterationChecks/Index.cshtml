@model IEnumerable<DentalClinic.Models.PatientRegisterationCheck>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="font-weight-light">Patient Registeration Check List</h1>
<br />
<p>
    <a asp-action="AddOrEdit" asp-controller="PatientRegisterationChecks"
       class="btn btn-outline-success btn-sm">
        <i class="fas fa-plus">
        </i> Add new
    </a>
</p>
<table class="table table-hover">
    <thead class="table-sm table-warning">
        <tr>
            <th>ID</th>
            <th>CHECK DATE</th>
            <th>PATIENT REGISTERATION</th>
            <th>OPTIONS</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-sm">
            <td>
                @Html.DisplayFor(modelItem => item.PatientRegisterationCheckId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PatientRegisteration.PatientRegisterationId)
            </td>
            <td>
                <span class="pr-1"><a asp-action="AddOrEdit" asp-route-id="@item.PatientRegisterationCheckId"><i class="fas fa-edit"></i> Edit </a></span>
                <span class="pl-1"><a asp-action="Delete" asp-route-id="@item.PatientRegisterationCheckId" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')"><i class="fas fa-trash"></i> Delete </a></span>
            </td>
        </tr>
}
    </tbody>
</table>
